<div id="ticket">
	<h2><%= @ticket.title %></h2>
	<%= link_to "Edit Ticket", [:edit, @project, @ticket] %>

	<%= simple_format(@ticket.description) %>
</div>

<%# ist das gleiche wie: edit_project_ticket_path(@project, @ticket) %>